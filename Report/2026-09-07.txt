# รายงานความคืบหน้า — วันที่ 7 กันยายน 2569
**โปรเจค:** ระบบยืม-คืนอุปกรณ์ (Equipment Borrowing System)
**ผู้รายงาน:** Wiraphat

---

## สิ่งที่ทำสำเร็จวันนี้ ✅

วันนี้ปิดแผน feedback อาจารย์ 5 ก.ย. ไป 3 เฟสรวด (เฟส 3, 4, 5) + งานแทรกเรื่องใบยืมที่เซ็นแล้ว
รวมแล้วแผน 9 เฟสเสร็จไป 6 เฟส (0–5) เหลือ 6–9

### Backend

**เฟส 3 — วันคืนรายชิ้น + อนุมัติ/ปฏิเสธบางชิ้น (feedback ข้อ 6, 8)**
- migration `0031_borrow_item_due_date_and_status.py` — `borrow_items` เพิ่ม
  `requested_due_date` / `due_date` / `item_status` / `rejection_reason`
- `app/utils/duedate.py::effective_due_date(item, req)` — จุดเดียวที่ตัดสิน "วันครบกำหนดจริง"
  (ลำดับ `extended_due_date` > `borrow_items.due_date` > `borrow_requests.due_date`)
  แทน `coalesce(...)` ที่เคยกระจายอยู่ใน `borrow_service.py` / `utils/scheduler.py` / `utils/pdf.py`
  — เดิม scheduler ทวงวันหนึ่ง หน้าเว็บโชว์อีกวัน ใบยืมพิมพ์อีกวัน
- `borrow_service.approve_request()` รับ body รายชิ้น
  `{"items":[{"item_id","approved","due_date","rejection_reason"}]}` — ไม่ส่ง = อนุมัติทั้งใบเหมือนเดิม
  · ตัดสต็อกเฉพาะชิ้นที่อนุมัติ · ไม่อนุมัติต้องมีเหตุผล (400 ถ้าเว้นว่าง)
- `_all_settled()` ข้ามชิ้น `item_status = "rejected"` ทุกจุดที่นับ "ครบทุกชิ้นหรือยัง"
  ไม่งั้นคำขอที่ปฏิเสธบางชิ้นจะไม่มีวันปิด
- `utils/pdf.py` ใบยืมมีคอลัมน์ "กำหนดคืน" รายบรรทัด + ใบคืนแสดง กำหนด / คืนจริง / ช้ากี่วัน
  · แก้บั๊กเก่า ReportLab ตัดชื่อ-รหัสยาวหายไปเงียบ ๆ (ใส่ `wordWrap="CJK"`)

**เฟส 4 — นัดรับของ / นัดคืนของ (feedback ข้อ 10, 12)**
- migration `0032_pickup_and_return_appointment.py` —
  `borrow_requests.pickup_at/pickup_location/pickup_note` (ระดับคำขอ จ่ายทั้งใบพร้อมกัน) +
  `borrow_items.return_appoint_at/return_appoint_location` (ระดับรายชิ้น เพราะคืนแยกชิ้นได้ตั้งแต่เฟส 3)
- settings ใหม่ `default_pickup_location` / `default_pickup_time` — ค่าเริ่มต้นในโมดัลไม่ hardcode
- `request_return_items()` บังคับกรอกวัน-เวลา-สถานที่นัดคืน (422 ถ้าไม่ส่ง, 400 ถ้านัดย้อนหลังเกิน 1 ชม.)
- `_to_utc()` — เวลาที่กรอกจากหน้าเว็บถือเป็นเวลาไทยแล้วแปลงเป็น UTC ก่อนเก็บ
  ไม่งั้นนัด 13:00 กลายเป็น 20:00 ในเอกสาร
- นัดรับของปรากฏครบทั้งแจ้งเตือน in-app, อีเมลอนุมัติ และใบยืม PDF

**เฟส 5 — สถิติความคุ้มค่า + เปลี่ยนกฎจ่ายของ (feedback ข้อ 11, 9)**
- `dashboard_service.get_utilization()` + `GET /dashboard/utilization` (require_admin)
  คำนวณสดจาก `borrow_items` ไม่เพิ่มคอลัมน์ใหม่เลย: ถูกยืมกี่ครั้ง / รวมกี่วัน /
  อัตราการใช้งาน (วันที่ถูกยืม ÷ วันที่ครอบครอง) / ต้นทุนต่อวันใช้งานจริง (`unit_value ÷ วันที่ถูกยืม`) /
  ป้าย good-fair-idle + สรุป "ซื้อมาแล้วไม่เคยถูกยืมเลย" พร้อมมูลค่ารวม
- `equipment_service.borrowed_days_expr()` — สูตร "วันที่ออกจากคลัง" อยู่ที่เดียว
  (นับขั้นต่ำ 1 วันต่อการยืม 1 ครั้ง, ปัดแบบ round ไม่ใช่ ceil — ยืม 10 วันกับอีก 3 วินาทีต้องได้ 10)
  ใช้ร่วมกันทั้งหน้าสถิติและกฎจ่ายของ ไม่งั้นหน้าสถิติบอกอย่าง ระบบจ่ายของอีกอย่าง
- `equipment_service.usage_days_map()` — aggregate วันที่ถูกยืมรายหน่วย
- **เปลี่ยนกฎเลือกหน่วยจ่ายของ**: `fifo_key()` → `dispatch_key()` =
  (วันที่เคยถูกยืมรวม **น้อยสุดก่อน**, `acquired_at` เก่าสุดก่อน, `code`)
  ยกเลิกกฎ "เก่าสุดให้ยืมก่อน" ที่ตกลงไว้ในแผน 3 ก.ย. โดยตั้งใจ — กฎเดิมทำให้ของเก่าที่โทรมอยู่แล้ว
  ถูกจ่ายซ้ำจนพังอยู่ชิ้นเดียว ขณะที่ชิ้นใหม่แทบไม่ถูกแตะ
  (แก้ที่ `borrow_service.py` 2 จุด: ตอนสร้างคำขอ และตอนอนุมัติ)
- `ApproveItemDecision.equipment_id` — แอดมิน **เลือกหน่วยเอง**ทับกฎอัตโนมัติได้ (ต้องอยู่ในรุ่นเดียวกัน
  ไม่งั้น 400) เคสจริงคือจ่ายเครื่องที่วางอยู่ตรงหน้าเคาน์เตอร์
- `EquipmentUnitSummary.days_borrowed` — หน้าจัดการ/โมดัลเลือกหน่วยเห็นว่าแต่ละหน่วยถูกใช้มาแล้วกี่วัน

**งานแทรก — ใบยืมที่เซ็นแล้ว (ผู้ใช้สั่งเพิ่มระหว่างวัน, เป็นเวอร์ชันย่อของเฟส 7.2)**
- migration `0033_signed_form_upload.py` — `borrow_requests.signed_form_url` / `signed_form_at`
- `POST /borrow-requests/{id}/signed-form` — เจ้าของคำขอหรือ staff อัปโหลดได้
  เฉพาะคำขอสถานะ `approved` · รับ `.pdf/.jpg/.jpeg/.png/.webp` ≤ 10MB · อัปใหม่ = ทับของเดิม
  · แจ้งเตือน in-app + อีเมลแอดมิน · ลง audit (`upload_signed_form`)
- `equipment_service.save_image()` แตกเป็น `save_upload(file, allowed_ext, max_bytes)`
  จุดตรวจนามสกุล/ขนาด/เขียนไฟล์อยู่ที่เดียว ไม่ต้องก๊อปโค้ดไปไว้ที่ `borrow_service`
- ย่อหน้า "อย่าลืม: ปริ้นใบยืมลงลายเซ็น…" แทรกในอีเมลอนุมัติ (`approve_request`)

### Frontend

- **หน้าใหม่ `/admin/utilization`** (`pages/admin/UtilizationPage.jsx`) — ตาราง sort ได้ทุกคอลัมน์
  (คลิกหัวคอลัมน์ ตัวเลขเรียงมาก→น้อยก่อน, ค่า null ไปท้ายแถวเสมอ) + ค้นหารหัส/ชื่อ +
  filter ประเภทและระดับความคุ้มค่า + การ์ดสรุป 3 ใบ
  · ย้ำในหน้าจอว่า "ใช้ประกอบการตัดสินใจจัดซื้อเท่านั้น ไม่ใช่ฐานคิดค่าปรับ"
- `api/dashboardApi.js` (ไฟล์ใหม่) — ย้าย `DashboardPage.jsx` ที่เคยเรียก `api.get()` ตรง ๆ
  มาใช้ผ่าน `api/` ตาม convention ใน CLAUDE.md
- `components/borrow/UnitPickerModal.jsx` (ไฟล์ใหม่) — โมดัลเลือกหน่วยจ่ายเอง
  เรียงจากหน่วยที่ถูกใช้น้อยสุด แสดง "ถูกใช้มาแล้ว N วัน · ได้มาเมื่อ…" หน่วยที่ไม่ว่างกดไม่ได้
- `pages/admin/BorrowRequestsPage.jsx` — ปุ่ม "เลือกหน่วยเอง / จ่ายหน่วย XXX (เปลี่ยน)" ต่อรายการ
  + โมดัลนัดรับของก่อนกดยืนยันอนุมัติ (เฟส 4)
- `pages/student/MyBorrowsPage.jsx` — แถบเหลืองในคำขอที่อนุมัติแล้ว บอกให้ปริ้นใบยืมลงลายเซ็น
  + ปุ่มอัปโหลดไฟล์ที่เซ็นแล้ว + ลิงก์เปิดดูไฟล์ที่ส่งไปแล้วพร้อมเวลาที่ส่ง
- `pages/admin/AllBorrowsPage.jsx` — ลิงก์ "📎 ใบยืมที่เซ็นแล้ว (เวลา)" ในรายละเอียดคำขอ
- `components/layout/Sidebar.jsx` — เมนู "สถิติความคุ้มค่า" · `App.jsx` route `/admin/utilization`
- `components/audit/auditLabels.js` — ป้ายไทยของ action `upload_signed_form` + field `file`
- `components/layout/NotificationBell.jsx` — แจ้งเตือน `signed_form_uploaded` พาไปหน้าประวัติการยืม

### อื่น ๆ (Config, Docker, Docs)

- `CLAUDE.md` — เพิ่มหัวข้อ "ลำดับจ่ายของในรุ่นที่มีหลายหน่วย" (ประกาศยกเลิกกฎเก่าชัดเจน
  พร้อมเหตุผล) และหัวข้อ "ใบยืมที่เซ็นแล้ว" · อัปเดต body ของ `/approve` ให้มี `equipment_id`
- `Plans/2026-09-05-feedback-อาจารย์-17-ข้อ.md` — มาร์คเฟส 3, 4, 5 เป็น ✅ พร้อมบันทึกส่วนที่ทำ
  ต่างจากแผน
- เทสใหม่: `test_signed_form_upload.py` (2) · `test_utilization_stats.py` (3) ·
  เขียน `test_equipment_fifo_allocation.py` ใหม่เป็น `test_equipment_dispatch_allocation.py`
  · แก้ `test_equipment_group_race.py` ให้ยืนยันกฎใหม่
- รวม **366 pytest ผ่านทั้งหมด** · `npm run build` ผ่าน

---

## ปัญหาที่พบ / ปัญหาที่ยังค้างอยู่ 🔴

| ปัญหา | สาเหตุ | วิธีแก้ / แผน |
|---|---|---|
| เทสเก่า 4 ตัวพังหลังเปลี่ยนกฎจ่ายของ | เทสเดิมยืนยันกฎ FIFO ("คืนของเก่าสุดแล้วต้องถูกจ่ายออกก่อนอีกครั้ง") ซึ่งตรงข้ามกับกฎใหม่ | เขียนเทสใหม่ให้ยืนยันกฎใหม่แทน (คืนแล้วต้องไปต่อท้ายคิว) ไม่ใช่แก้โค้ดกลับ — เป็นการเปลี่ยนพฤติกรรมโดยตั้งใจ |
| นับวันด้วย `ceil` ทำให้ยืม 10 วันกลายเป็น 11 | `returned_at` ช้ากว่า `approved_at + 10 วัน` ไปไม่กี่ไมโครวินาที `ceil` ปัดขึ้นทันที | เปลี่ยนเป็น `round` + `greatest(1, …)` — ยืมครึ่งวันยังนับ 1 วัน แต่ 10 วันเป๊ะได้ 10 |
| ไฟล์ใบยืมที่เซ็นแล้วอยู่ใน `/uploads` ที่ mount เป็น static | ใช้กลไกเดิมของรูปอุปกรณ์ ซึ่งเปิดสาธารณะ ใครรู้ URL ก็เปิดได้ (ชื่อไฟล์เป็น uuid เดาไม่ได้ แต่ไม่มีการตรวจสิทธิ์) | **เฟส 7.1** ย้ายไป `PRIVATE_UPLOAD_DIR` + route ตรวจสิทธิ์ + ตรวจ magic bytes — มาร์ค `ponytail:` ไว้ในโค้ดแล้วว่าเป็นหนี้ที่ตั้งใจ |
| dev DB มีอุปกรณ์ 1,462 จาก 1,488 แถวที่ "ไม่เคยถูกยืมเลย" และเกือบทุกแถวไม่มี `acquired_at` | ยังไม่ได้ backfill ข้อมูลทะเบียนจริง | หน้าสถิติแสดง "—" แทนอัตราการใช้งานเมื่อไม่มีวันที่ได้มา (ไม่พัง) แต่ตัวเลขจะมีความหมายจริงต่อเมื่อ backfill แล้ว — จำเป็นต่อเฟส 6 ด้วย |
| งานทั้งหมดตั้งแต่ 5 ก.ย. ยัง uncommitted (136 ไฟล์) | ผู้ใช้ยังไม่สั่ง commit | รอผู้ใช้สั่ง — diff ใหญ่มาก ควร commit แยกเป็นเฟส ๆ |

---

## แผนวันพรุ่งนี้ 📋

1. [ ] ทดลองใช้จริงหน้า `/admin/utilization` กับข้อมูล dev แล้วดูว่าตัวเลข "ต้นทุนต่อวันใช้งาน"
       สื่อความหมายพอไหม (ตอนนี้ของ 1,462 แถวขึ้น "ไม่ถูกใช้" หมด อาจดูน่าตกใจเกินจริง)
2. [ ] เริ่ม **เฟส 6 — ค่าปรับล่าช้า + ค่าเสียหาย** ตามแผน:
       migration เพิ่ม `borrow_items.fine_days_late / fine_late_amount / fine_damage_amount /
       fine_basis / fine_status / fine_waived_by / fine_waived_reason` +
       settings `fine_per_day_per_item` / `fine_grace_days` / `fine_max_per_item`
3. [ ] คำนวณค่าปรับแบบ **freeze ตอนปิดรายการ** (ไม่คำนวณสด) ใน `borrow_service.return_item()`
       — ใช้ `effective_due_date` เป็นฐานเสมอ ห้าม coalesce เอง
4. [ ] โมดัลรับคืน (`components/borrow/ReturnModal.jsx`) โชว์ยอดค่าปรับพร้อมที่มา
       ("ล่าช้า 5 วัน × 10 บาท = 50 บาท") + ช่องแก้ยอด + ปุ่มยกเว้น (superadmin)
5. [ ] แก้ `CLAUDE.md` หัวข้อ 11 ที่ยังเขียนว่าค่าปรับอยู่นอกขอบเขต ให้ตรงกับสิ่งที่ทำจริง
6. [ ] ถ้าเวลาพอ: เฟส 7.1 ปิดช่องโหว่ไฟล์ใบเซ็น (งานเล็ก แต่เป็นความเสี่ยงด้านความเป็นส่วนตัว)

---

## สถานะโดยรวม

**Backend:** ~99% ของขอบเขตเดิม + เฟสใหม่ **6 ใน 9 เฟสเสร็จ** (0–5) — เหลือ 6 ค่าปรับ /
7 ความปลอดภัยไฟล์ใบเซ็น / 8 ฟอร์มอุปกรณ์+มาตรฐานชื่อ / 9 predefine ผู้สมัคร
**Frontend:** ~98% ของขอบเขตเดิม + หน้าใหม่รวม 3 หน้า (คำขอแก้ไขข้อมูล, ตรวจสอบระบบ, สถิติความคุ้มค่า)
**ระยะรวม:** แผนใหม่ประเมิน 17–19 วันทำงาน ทำไปแล้ว ~7 วันทำงานเทียบเท่า (เฟส 0–5)
เทสอัตโนมัติ 366 ตัวผ่านทั้งหมด (จาก 345 เมื่อ 5 ก.ย.)

---

## หมายเหตุ / ความเสี่ยง ⚠️

- **Pending Items จาก CLAUDE.md ข้อ 10 ที่ยังค้าง:**
  - ไฟล์รายชื่อนักศึกษาจากสาขา — จำเป็นสำหรับเฟส 9 (predefine ผู้สมัคร) ยังไม่ได้รับ
    ถ้าไม่ได้จะทำเป็นคิวอนุมัติผู้สมัครแทน
  - `ALLOWED_EMAIL_DOMAINS` ยังต้องยืนยันกับอาจารย์ก่อน deploy จริง
  - LINE OA ยังไม่เริ่ม (stretch goal)
- **กฎจ่ายของเปลี่ยนไปแล้วจริง ๆ วันนี้** — จาก "เก่าสุดก่อน" (ตัดสินใจ 3 ก.ย.) เป็น
  "ถูกใช้น้อยสุดก่อน" (feedback 5 ก.ย. ข้อ 9) บันทึกไว้ใน `CLAUDE.md` และ memory แล้ว
  เพื่อไม่ให้รอบหน้าแก้กลับไปกลับมา — ถ้าอาจารย์ต้องการกฎเดิมคืน ต้องแก้ที่ `dispatch_key()` จุดเดียว
- **ค่าปรับ (เฟส 6) ยังขัดกับ CLAUDE.md ข้อ 11** ที่เขียนว่าอยู่นอกขอบเขต — ต้องแก้เอกสาร
  พร้อมกับตอนทำ ขอบเขตที่ตกลงคือคิดและเก็บประวัติเท่านั้น **ยังไม่บล็อกการยืมครั้งต่อไป**
- **ข้อมูลทะเบียนไม่ครบเป็นคอขวดของเฟส 6** — ค่าเสียหายคิดจากมูลค่าตามบัญชี ซึ่งต้องมีทั้ง
  `unit_value` และ `acquired_at` ตอนนี้ dev DB ยังขาดเกือบทั้งหมด ควรเริ่ม backfill ควบคู่ไปด้วย
- ใบยืมที่เซ็นแล้ว **ไม่บล็อกการจ่ายของ** โดยตั้งใจ (ของจริงเซ็นกันหน้าเคาน์เตอร์)
  เป็นแค่หลักฐานประกอบ ถ้าอาจารย์ต้องการให้บังคับต้องคุยเรื่องขั้นตอนหน้างานก่อน
