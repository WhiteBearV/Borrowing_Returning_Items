# รายงานความคืบหน้า — วันที่ 21 กันยายน 2569
**โปรเจค:** ระบบยืม-คืนอุปกรณ์ (Equipment Borrowing System)
**ผู้รายงาน:** Wiraphat

สรุปสั้น: ย้าย VM แล้ว IP เปลี่ยนเป็น 172.16.46.134 ทำให้ container ทั้งหมดดับ — วิเคราะห์สาเหตุ + สอนขั้นตอน
เปิดระบบ/สำรองข้อมูลก่อนอัปเดต จากนั้นแก้บั๊กและเพิ่มฟีเจอร์ตาม feedback ต่อเนื่องทั้งวัน (วันที่ วว/ดด/ปปปป ทุกหน้า,
เวลาไทยทั้งระบบ, ตั้งเวลาส่งแจ้งเตือน, สูตรต้นทุนการใช้งานใหม่ตามหลักสากล, เลือกช่วงเวลาในหน้าความคุ้มค่า,
ฟอร์มแก้ไขหลายรายการใหม่) เทสเพิ่มจาก 451 → 457 ผ่านทั้งหมด · commit + push แล้ว `1da1c22` (103 ไฟล์)
**ยังไม่ได้ deploy ขึ้น VM**

---

## สิ่งที่ทำสำเร็จวันนี้ ✅

### Server / VM (วิเคราะห์ + สอน ไม่ได้แก้บนเครื่องจริง)
- ตรวจ VM `DockerMaeb` (172.16.46.134): Docker daemon ทำงานปกติ แต่ container ทั้ง 3 ตัว `Exited` ตั้งแต่ 10 ก.ย.
  — ถูกสั่งหยุดก่อนย้าย (`restart: unless-stopped` จึงไม่เปิดให้เอง) · `.env` (`FRONTEND_URL`/`APP_BASE_URL`)
  และ cert HTTPS ยังเป็น IP เก่า .130 · โค้ดบน VM ยังเป็น `695f9b7`
- สอนขั้นตอนเปิดระบบ: `gen-self-signed-cert.sh <ip>` → แก้ `.env` → `docker compose -f docker-compose.prod.yml up -d`
  → ตรวจ `ps` / `curl https://localhost/api/health`
- สอนการสำรองข้อมูลก่อนอัปเดต: `BACKUP_DIR=... bash backend/scripts/backup_db.sh` + สำรอง `.env`/`certs`/เลข commit
  → ตรวจไฟล์ด้วย `pg_restore -l` / `tar tzf` → คัดลอกออกนอก VM ด้วย `scp` → ขั้นตอนย้อนกลับ (ย้อนโค้ดก่อน แล้วค่อย `restore_db.sh`)
- อธิบาย named volume vs bind mount (รูปอยู่ที่ `/var/lib/docker/volumes/..._uploads_data/_data` เป็นโฟลเดอร์ธรรมดา)

### Backend
- **แก้บั๊ก audit log พัง 500 เมื่อ detail มีวันที่** — `audit_service._jsonable()` รองรับ dict แบบ recursive และ
  `log_action()` ผ่าน `_jsonable(detail)` ทุกครั้ง (เจอจากเทสตอนแก้ bulk edit)
- **แก้บั๊ก bulk edit วันที่ได้มา/อายุการใช้งานไม่เคยบันทึก** — `schemas/equipment.py::EquipmentBulkUpdate` ไม่มี
  `acquired_at`/`useful_life_years`/`book_value_override` pydantic ทิ้งเงียบ ๆ แต่ตอบ "สำเร็จ" → เพิ่มฟิลด์ (validator
  เดียวกับ `EquipmentUpdate`) ยังกั้นด้วย `FINANCE_FIELDS` = superadmin เท่านั้น
- **เวลาไทยทั้งระบบ** — `backend/Dockerfile` เพิ่ม `ENV TZ=Asia/Bangkok` (container เป็น UTC: job ตรวจเกินกำหนดที่ยิง
  เที่ยงคืนไทยเทียบ `date.today()` ของเมื่อวาน → ทวงช้าไป 1 วัน) ตรวจแล้วว่า image มี tzdata
- **วันที่ในแจ้งเตือน/อีเมลเป็น วว/ดด/ปปปป** — `utils/duedate.py::fmt_date()` ใช้ใน `borrow_service.py`
  (อนุมัติ/ต่อเวลา/ทวง) และ `utils/scheduler.py` · `_fmt_appoint()` จาก `18/9/2569` → `18/09/2026`
- **setting `notify_time` (เวลาส่งแจ้งเตือนรายวัน)** — migration `0041_notify_time_setting.py` (ค่าเริ่มต้น 08:00) ·
  `scheduler.py` อ่านค่าตอนบูต (`start_scheduler()` เป็น async) + `reschedule_daily_jobs()` · `settings_service.py`
  ตรวจรูปแบบ HH:MM แล้วเลื่อน job หลัง commit ทันทีไม่ต้องรีสตาร์ต · superadmin เท่านั้น
- **รายชื่อนักศึกษาที่รับรองกรองตามรุ่น** — `GET /eligible-students?cohort=66` + คืน `cohorts` (รุ่น → จำนวนคน) ใน
  `student_import_service.list_students()`
- **สูตรต้นทุนการใช้งานใหม่** (ค้นข้อมูลหลักสากล: กรมบัญชีกลาง, recharge rate ของมหาวิทยาลัย, ownership cost, cost per use)
  - `equipment_service.daily_depreciation()` + `life_days()` จุดเดียว (`book_value()` เรียกใช้ร่วมกัน)
  - `dashboard_service.get_utilization()`: ต้นทุน/วันที่ใช้ = ค่าเสื่อมในช่วงที่วัดผล ÷ วันที่ถูกยืม (เดิม ราคาทั้งก้อน ÷ วันยืม
    → เครื่องแม่ข่าย 381,776 บ. ยืม 2 วัน ขึ้น 190,888 บ./วัน · ตอนนี้ 7,734.80 บ. + ค่าเสื่อม 209.05 บ./วัน)
  - ช่วงวัดผลนับตั้งแต่ของเข้าระบบ (หรือวันที่ได้มาถ้าช้ากว่า) · ป้ายใหม่ `low` "ใช้งานน้อย" แยกจาก `idle` "ไม่เคยถูกยืม"
  - **เลือกช่วงเวลาได้** (`date_from`/`date_to`) — ต่อยอด `borrowed_days_expr(since, until)` + `overlaps_window()`
    (ไม่เขียนสูตรนับวันซ้ำตาม CLAUDE.md) · สรุปรายเดือน `_monthly_usage()` (ยืมใหม่/วันออกจากคลัง/มูลค่าที่ถูกยืมออก)
  - ส่งฐานการคำนวณ (อายุ/ซาก/เกณฑ์) ให้หน้าเว็บเขียนคำอธิบายจากค่าจริง
- **การ์ด Dashboard** — `borrowed_value_this_month/year` = มูลค่าอุปกรณ์ทุกประเภทที่ถูกยืมออก (ตามวันที่อนุมัติ ราคา ณ วันอนุมัติ)
  แทนสูตรเดิมที่นับแค่วัสดุสิ้นเปลืองที่สรุปผลว่าใช้หมด/ทิ้งแล้ว (ผู้ใช้เลือกเอง)
- PDF ใบรับเข้า/ปลดระวาง: ประเภท "วัสดุ" → "วัสดุใช้ซ้ำ" (`pdf.py` ใช้ `_ITEM_TYPE_TH` ตัวเดียว)
- เทสใหม่/แก้: `test_notify_time.py`, cohort filter, bulk finance fields, utilization (สูตรใหม่/ป้าย low/เลือกช่วง/
  รายเดือน = การ์ด Dashboard) — **457 passed**

### Frontend
- **แก้บั๊กสร้างอุปกรณ์ใหม่ค้าง "กำลังบันทึก…"** — `EquipmentManagePage.jsx::submit` อ่าน `initial.quality_tracked`
  ทั้งที่ `initial` เป็น null ตอนเพิ่มใหม่ → throw นอก try ปุ่มค้างตลอดไป (บั๊กเฉพาะโค้ดเฟส 10 ที่ยังไม่ขึ้น VM)
- **วันที่ วว/ดด/ปปปป ทุกหน้า** — `components/common/DateInput.jsx` แทน `<input type="date|datetime-local">` 12 จุด
  (เบราว์เซอร์ภาษาอังกฤษโชว์ เดือน/วัน/ปี และบังคับผ่าน attribute ไม่ได้) · `toLocaleString('th-TH')` (พ.ศ. ไม่ zero-pad)
  → `formatDateTime` ในหน้า Audit/คำขอแก้ไข/ตรวจสอบระบบ/ไทม์ไลน์/รายการคำขอ · ค่า timestamp ใน audit detail
- **เวลาไทยเสมอ** — `utils/formatDate.js`: `todayTH()` / `daysSinceTH()` + `formatDate`/`formatDateTime` แปลงเป็นเวลาไทยเอง
  ไม่ขึ้นกับ timezone เครื่อง (แก้ "วันนี้" ที่คิดแบบ UTC ~9 จุด, วันเกินกำหนด, พรีวิวค่าปรับ `fine.js`/`dueDate.js`)
  · สคริปต์เช็ค `utils/formatDate.check.mjs` รันผ่าน 3 timezone
- **ฟอร์มแก้ไขหลายรายการใหม่** (`BulkEditModal`) — หน้าตาเดียวกับแก้รายชิ้น (3 แท็บ/2 คอลัมน์/ลำดับช่องเดียวกัน)
  ทุกช่องมีสวิตช์ "แก้ไข" ไม่ติ๊ก = สีเทา "คงค่าเดิม" ฟอร์มไม่ยืด · แถบสีอำพัน + ป้าย "N รายการ" + ตัวเลขบนแท็บ
  · ฟอร์มรายชิ้นเพิ่ม "แจ้งเตือนของใกล้หมด" / หลายรายการเพิ่ม "มูลค่าตามบัญชี"
- **หน้าสถิติความคุ้มค่า** (`UtilizationPage.jsx` เขียนใหม่) — การ์ดสูตร 4 ใบด้านบน, เลือกช่วง (ทั้งหมด/เดือนนี้/ปีนี้/เลือกเอง)
  เก็บใน URL, ภาพรวมรายเดือนพร้อมแท่ง (กดเดือนเพื่อเจาะดู), คอลัมน์ "อยู่ในระบบ (วัน)", แบ่งหน้าละ 15 รายการ
- **Dashboard** — การ์ดมูลค่าที่ถูกยืมออกเดือน/ปีนี้กดเข้าหน้าความคุ้มค่าพร้อมช่วงเวลา
- **Settings** — ช่องเวลาส่งแจ้งเตือน (superadmin) · ช่องรูปแบบ HH:MM เป็นกล่องแคบ
- **รายชื่อนักศึกษาที่รับรอง** — dropdown กรองตามรุ่น "รุ่น 64 (8 คน) … รุ่น 69 (23 คน)"
- "วัสดุ" → "วัสดุใช้ซ้ำ" ทุกจุด (Dashboard, ฟอร์ม/ตัวกรองอุปกรณ์, หน้านักศึกษา) · จุดที่หมายถึงวัสดุสิ้นเปลืองแก้เป็นคำเต็ม

### อื่น ๆ (Config, Docker, Docs)
- `CLAUDE.md`: กฎเวลาไทย + `notify_time` + สูตรหน้าความคุ้มค่า/เลือกช่วง/นิยามการ์ด Dashboard
- `docs/02-system-design.md`: เพิ่ม `notify_time` ในตาราง settings + หัวข้องานพื้นหลัง
- `.gitignore`: `docs/repofficeradvisor*.xls` (รายชื่อนักศึกษาจริง รหัส+ชื่อ) — repo GitHub เป็น public
- commit + push `1da1c22` บน main (103 ไฟล์ รวมงานเฟส 10 ที่ค้าง commit ตั้งแต่ 18 ก.ย.)

---

## ปัญหาที่พบ / ปัญหาที่ยังค้างอยู่ 🔴

| ปัญหา | สาเหตุ | วิธีแก้ / แผน |
|---|---|---|
| ระบบบน VM ดับทั้งหมด | container ถูกสั่งหยุดก่อนย้าย VM (unless-stopped ไม่เปิดให้) + IP เปลี่ยนเป็น .134 | สอนขั้นตอนเปิดระบบแล้ว รอผู้ใช้ทำ/สั่งให้ทำ พร้อม deploy `1da1c22` |
| backup อัตโนมัติรายวันล้มทุกคืนตั้งแต่ 11 ก.ย. | `backup_db.sh` ใช้ `docker compose exec` แต่ db ไม่รัน → ได้ไฟล์ 0 ไบต์ | จะหายเองเมื่อเปิดระบบ · ไฟล์ดีล่าสุด 10 ก.ย. (ข้อมูลไม่เปลี่ยนตั้งแต่นั้น) |
| `backup_db.sh` ไม่สำรอง volume `private_uploads_data` (ใบยืมที่เซ็นแล้ว) + ทิ้งไฟล์ 0 ไบต์ตอนล้ม | สคริปต์เขียนก่อนมีฟีเจอร์ใบยืมที่เซ็นแล้ว | เสนอแก้แล้ว ยังไม่ได้สั่ง — ควรทำก่อน deploy |
| backup อยู่บน VM เครื่องเดียว | ยังไม่มี off-site | สอน `scp` ออกมาเก็บเองก่อน · เฟส 6 (off-site) ยังค้าง |
| นาฬิกา VM เป็น UTC | ค่าเริ่มต้นของเครื่อง | cron "ตี 2" รันจริง 09:00 น. — แนะนำ `timedatectl set-timezone Asia/Bangkok` |
| อุปกรณ์ 1,472/1,473 ชิ้นไม่มีวันที่ได้มา | ไม่ได้กรอกตอนนำเข้า | ค่าเสื่อมสมมติว่ายังอยู่ในอายุทุกชิ้น · bulk edit วันที่ได้มาใช้ได้แล้ว หรือเดาปีงบจากรหัส `64-213-…` (รอผู้ใช้ตัดสินใจ) |
| บั๊กของตัวเองระหว่างทำ: การ์ด Dashboard ได้ query กับ `'06-01'` | ตัวแปร `year_start` ซ้ำชื่อกับวันเริ่มปีการศึกษาในฟังก์ชันเดียวกัน | เทสจับได้ แก้ก่อนส่ง (query ทันทีไม่ lazy) |
| หน้า Audit ขาว `formatDateTime is not defined` ระหว่างแก้ | Vite HMR cache โมดูลตอนแก้ไฟล์ค้างครึ่งทาง | `touch` ไฟล์ให้ transform ใหม่ — ไม่ใช่บั๊กในโค้ด |
| ไฟล์รายชื่อนักศึกษาเกือบถูก push | ผู้ใช้สั่ง "commit ทั้งหมด" ไฟล์ .xls อยู่ใน docs/ และ repo เป็น public | ไม่ push + ใส่ .gitignore · ถ้าต้องเก็บใน repo ให้เปลี่ยนเป็น private ก่อน |

---

## แผนวันพรุ่งนี้ 📋

1. [ ] แก้ `backend/scripts/backup_db.sh` ให้สำรอง `private_uploads_data` + ไม่ทิ้งไฟล์ 0 ไบต์เมื่อ backup ล้ม
2. [ ] เปิดระบบบน VM (.134): cert ใหม่ + แก้ `.env` → `up -d` → **backup + ตรวจไฟล์ + scp ออกมา**
3. [ ] Deploy `1da1c22`: `git pull` → `up --build -d` (migration 0023 → 0041) → ตรวจ health/log/ข้อมูลครบ
4. [ ] QA บน VM: สร้างอุปกรณ์ใหม่, แก้ไขหลายรายการ (วันที่ได้มา), หน้าความคุ้มค่า (เลือกช่วง/รายเดือน), ตั้ง `notify_time`
5. [ ] `timedatectl set-timezone Asia/Bangkok` บน VM (ถ้าผู้ใช้ตกลง)
6. [ ] ตัดสินใจเรื่องวันที่ได้มาของอุปกรณ์ (กรอกผ่าน bulk edit หรือเดาจากรหัสทะเบียน)

---

## สถานะโดยรวม

**Backend:** ~99% — ฟีเจอร์ครบ เทส 457 ผ่าน เหลือแก้สคริปต์ backup + backup off-site
**Frontend:** ~98% — ทุกหน้าที่แก้วันนี้ QA บนเบราว์เซอร์ (dev) แล้ว เหลือ QA ซ้ำบน VM หลัง deploy
**ระยะรวม:** โค้ดพร้อม deploy แล้ว (push ขึ้น main) — ติดที่ต้องเปิดระบบบน VM + สำรองข้อมูลก่อนอัปเดต

---

## หมายเหตุ / ความเสี่ยง ⚠️

- **deploy รอบนี้ migration เยอะ (0023 → 0041)** — ต้อง backup และตรวจไฟล์ก่อนทุกครั้ง ถ้าพังให้ย้อนโค้ดก่อนแล้วค่อยกู้ DB
- **พฤติกรรมที่เปลี่ยนหลัง deploy:** แจ้งเตือน/อีเมลรายวันส่ง **08:00** (เดิมเที่ยงคืน) · การ์ด Dashboard เปลี่ยนความหมายเป็น
  "มูลค่าอุปกรณ์ที่ถูกยืมออก" (ไม่ใช่เงินที่เสียไป ของที่ยืมหลายครั้งนับทุกครั้ง) · ปีค.ศ. ในวันที่ทุกหน้า (ไม่ใช่ พ.ศ.)
- ตอนเปิดระบบต้องแก้ `FRONTEND_URL`/`APP_BASE_URL` เป็น `https://172.16.46.134` ไม่งั้นลิงก์ในอีเมลยืนยัน/รีเซ็ตรหัสผ่านกดไม่ได้
- Pending Items (CLAUDE.md หัวข้อ 10): ยืนยัน `ALLOWED_EMAIL_DOMAINS` กับอาจารย์ · LINE OA ยังเป็น stretch goal
