# รายงานความคืบหน้า — วันที่ 23 กันยายน 2569
**โปรเจค:** ระบบยืม-คืนอุปกรณ์ (Equipment Borrowing System)
**ผู้รายงาน:** Wiraphat

สรุปสั้น: วันนี้ได้ระบบตัวอย่าง **SKGRent** (ระบบให้เช่าเครื่องมือเชิงพาณิชย์ เขียนด้วย Clarion + SQL Server)
มาแกะดู — ดึง schema 95 ตารางและ **สูตรคำนวณจริงจากซอร์สที่ฝังในตัวโปรแกรม** ออกมาเทียบกับระบบเรา
แล้วลงมือทำ 3 งาน: กระทบยอดสต็อกในหน้าตรวจสอบระบบ · รหัสประจำตัวบุคลากรตามที่อาจารย์ขอ ·
**เฟส 11 เซ็นรับของบนหน้าจอ** (ทำเสร็จทั้งวงจร ตั้งแต่ canvas จนถึงลายเซ็นบนใบยืม PDF)
เทสเพิ่มจาก 462 → **470 ผ่านทั้งหมด** · ยังไม่ commit (มีงานค้าง 4 ก้อนในสายเดียว)

---

## สิ่งที่ทำสำเร็จวันนี้ ✅

### วิเคราะห์ระบบตัวอย่าง SKGRent (ของที่ได้มาใหม่)
- ไฟล์ `docs/MNKPJ/drive-download-.../` = ระบบให้เช่าเครื่องมือ Clarion (dictionary ปี 2543 แก้ล่าสุด 2555)
  + ฐาน SQL Server 2 ตัว (สาขาอ่อนนุช 628 MB / ศูนย์กลาง)
- แกะ `SKGData.txd` (dictionary แบบข้อความ เข้ารหัส cp874 + escape แบบ Clarion) ได้ **schema ครบ 95 ตาราง
  พร้อมป้ายภาษาไทยทุกฟิลด์** และดึง **ซอร์สที่ฝังใน `SKGRent.app`** ออกมาได้ → ได้สูตรจริงไม่ต้องเดา
- เขียนบันทึกวิเคราะห์ `Plans/2026-09-23-skgrent-analysis.md` — 7 ข้อที่น่าหยิบมาใช้ + หัวข้อสูตรคำนวณ
  - **ตรงกับเราอยู่แล้ว:** ชิ้น-วัน (piece-days) · ขั้นต่ำ 1 วันต่อการยืม · freeze อัตรา ณ วันออกเอกสาร ·
    แยกประเภทค่าปรับแล้วค่อยรวม
  - **เขามีเราไม่มี:** สมการสมดุลสต็อก (`TotalIN − TotalOUT = มีอยู่ + ให้เช่า + ซ่อมอยู่ + จอง`) ·
    ปรับ "จำนวนวัน" แทนปรับ "ยอดเงิน" · เวลาตัดวันเป็น setting · คืนแยกสภาพในรายการเดียว · ยกเลิกแทนลบ
  - **เราดีกว่า:** ค่าเสื่อม/มูลค่าตามบัญชี/ต้นทุนต่อวัน (ระบบเขาไม่มีเลย — ค้นได้ 0 จุด) · ค่าคุณภาพ+ชั้นปี ·
    สูตรอยู่จุดเดียว (ของเขาสูตรค่าเช่าซ้ำหลายที่ในฟอร์ม/รายงาน)
  - ⚠️ พบว่าระบบเขาเก็บรหัสผ่านเป็น plain text + มีรหัสลับข้ามสิทธิ์ (`GODPWD` = 'Imgod') — บันทึกไว้ว่าห้ามลอก
- ตอบคำถามเรื่อง "อินดิเชียรเนม" ที่อาจารย์ขอ: เขาแยก 3 ชั้น (รหัสที่ระบบออกให้ / เลขที่ราชการออกให้ +
  ชนิดของเลข / ประเภทบุคคล) เขียนเป็นข้อ 7 ในไฟล์วิเคราะห์

### Backend — ชุดเล็ก (2 งาน)
- **กระทบยอดสต็อก** `system_check_service._stock_reconcile()` (ใหม่)
  - สมการ: `จำนวนทั้งหมด = คงเหลือ + ยืมค้าง + ที่คืนแบบไม่เข้าคลัง` (ใช้ `STOCK_RETURN_CONDITIONS` ของ
    `borrow_service` ตัวเดิม ไม่ได้ก๊อปสูตรใหม่)
  - ตรวจเฉพาะแถว `status = 'available'` — ของส่งซ่อม/สูญหายถูกกันยอดเป็น 0 โดยตั้งใจ ถ้านับรวมจะเตือนผิด 50 แถว
  - `add_issue()` รับ `sample` เพิ่ม → หน้าเว็บโชว์รหัสตัวอย่างได้ (รู้ว่าเพี้ยนแต่ไม่รู้ว่าแถวไหนก็ตามแก้ไม่ได้)
  - **ผลจริงบน dev DB: จาก 1,477 แถว เจอ 1 รายการ — `CON-WIRE-001` (มี 40 คงเหลือ 38 ยืมค้าง 1 → หาย 1
    โดยไม่มีประวัติรองรับ)** น่าจะเป็นเศษจากบั๊ก import เดิมที่ยังไม่ได้แก้ข้อมูล
- **รหัสประจำตัวบุคลากร** (feedback อาจารย์ — "อินดิเชียรเนม" เช่น `01MNK01`)
  - `schemas/user.py`: `STAFF_CODE_PATTERN` + `_require_staff_code()` — บังคับกรอก `username` เมื่อ role ≠ student
    และ **ห้ามเป็นตัวเลขล้วน** (ปนกับรหัสนักศึกษาไม่ได้ ทั้งบนเอกสารและตอนล็อกอิน)
  - ไม่บังคับรูปแบบเป๊ะ เพราะบัญชีเดิม (`Admin`, `SUAdmin`) ต้องยังแก้ไขได้

### Backend — เฟส 11: เซ็นรับของบนหน้าจอ (งานหลักของวัน)
- **migration 0042** `0042_handover_and_signatures.py` — `borrow_requests` เพิ่ม `handover_at` / `handover_by` /
  ลายเซ็น 4 คอลัมน์ (`handover_sig_*`, `return_sig_*`) / `signature_meta` (JSONB)
- `models/borrow_request.py`: `SIGNATURE_FIELDS` (mapping kind → คอลัมน์ **จุดเดียว**) + property `signatures`
  (ส่งชนิดที่เซ็นแล้ว ไม่ใช่ชื่อไฟล์) + `handover_by_name` + relationship `handover_staff`
- `borrow_service.py`: `_save_signature()` (เก็บไฟล์ + sha256 + ผู้เซ็น + เวลา + user-agent ลง `signature_meta`) ·
  `handover_request()` · `sign_return()` · `get_signature()`
  - ใช้ `equipment_service.save_private_upload()` จุดเดิม — PNG ≤1MB ใน `PRIVATE_UPLOAD_DIR` ไม่มี URL สาธารณะ
  - **ไม่แตะสต็อกและไม่เพิ่มค่าใน `status`** — ของออกจากคลังตั้งแต่อนุมัติตามกฎเดิมใน CLAUDE.md
  - **เซ็นได้ครั้งเดียว** (ตามที่ผู้ใช้สั่ง) เซ็นซ้ำ = 400
- `routers/borrow.py`: `POST /{id}/handover` · `POST /{id}/sign-return` (staff เท่านั้น) ·
  `GET /{id}/signature/{kind}` (เจ้าของคำขอหรือ staff, **ลง audit ทุกครั้งที่เปิดดู**)
- `utils/pdf.py`: `_signature_image()` (วางรูปทับเส้นประ คงสัดส่วนจริง) · `_signed_date_line()` (วันที่จาก
  `signature_meta[kind].signed_at` = **วันที่รับลายเซ็นนั้นจริง ๆ** ไม่ใช่เวลาจ่ายของรวม) · ป้ายท้ายเอกสาร
  "เอกสารฉบับนี้ลงลายมือชื่ออิเล็กทรอนิกส์ในระบบแล้ว"

### Frontend
- `components/common/SignaturePad.jsx` (ใหม่) — canvas เขียนเอง ~90 บรรทัด **ไม่เพิ่ม dependency**
  pointer events ครอบทั้งนิ้ว/เมาส์/ปากกา · `touchAction: none` กันหน้าเลื่อนตอนเซ็นบนมือถือ ·
  คูณ devicePixelRatio ให้เส้นคม · ปิดปุ่มยืนยันถ้ายังไม่มีเส้น
- `components/borrow/SignatureModal.jsx` (ใหม่) — ใช้ตัวเดียวทั้งจ่ายของและรับคืน + `SIGNATURE_LABEL` ที่ใช้ร่วมกัน
- `pages/admin/AllBorrowsPage.jsx` — ปุ่ม "จ่ายของ + เซ็นรับ" / "เซ็นรับคืน" (ซ่อนเมื่อเซ็นแล้ว) ·
  บรรทัด "จ่ายของแล้ว … โดย …" + ปุ่มเปิดดูลายเซ็นแต่ละฝั่ง
- `api/borrowApi.js` — `handover` / `signReturn` / `downloadSignature`
- `components/audit/auditLabels.js` — ป้ายไทยของ action ใหม่ 3 ตัว + `FIELD_LABEL` ของคีย์ detail ใหม่
  (กฎบังคับในโปรเจกต์: เพิ่ม action ต้องเพิ่มป้ายไทยเสมอ)
- `NotificationBell.jsx` — แจ้งเตือน `handover_done` พาไปหน้ารายการยืมของผู้ยืม
- `SystemCheckPage.jsx` / `UsersPage.jsx` — แสดง sample ของรายการที่ต้องตรวจ · ป้าย "รหัสนักศึกษา/รหัสบุคลากร"
  ใต้รหัส · แก้บั๊กที่ส่ง `username` เฉพาะตอน role = admin (สร้าง superadmin แล้วรหัสหายเงียบ ๆ)

### เทส (462 → 470 ผ่านทั้งหมด)
- `tests/test_handover_signature.py` (ใหม่ 4 เคส): เก็บลายเซ็นโดยสต็อกไม่ขยับ · ชื่อไฟล์ไม่หลุดออก API ·
  นักศึกษากดเองไม่ได้ (403) · ยังไม่อนุมัติจ่ายไม่ได้ (400) · ไฟล์ปลอม PNG ถูกปฏิเสธ · เซ็นซ้ำ 400 ·
  ใบคืน PDF มีป้ายลายมือชื่ออิเล็กทรอนิกส์ + วันที่ถูกเติมจริง
- `tests/test_stock_reconcile.py` (ใหม่ 3 เคส): จับของที่ยอดไม่ตรง · ไม่เตือนของส่งซ่อม · ไม่เตือนของที่ยอดตรง
- `tests/test_user_username.py::test_staff_code_rules` + ปรับเทสเดิม 2 ตัวที่เคยคาด 400 ให้ยังพิสูจน์เรื่องเดิมได้

### เอกสาร
- `CLAUDE.md` — เพิ่มกฎเฟส 11 ครบชุด (เซ็นครั้งเดียว · ไม่แตะสต็อก · วันที่จากลายเซ็นจริง · `Field(exclude=True)` ·
  `_signature_image` ต้องให้ PIL อ่านไฟล์ก่อน) + เพิ่ม action ใหม่ 3 ตัวในรายการ audit
- `Plans/2026-09-23-esignature-and-feature-gap.md` — แผนเฟส 11 + เทียบฟีเจอร์มาตรฐานกับคู่เทียบ 6 ระบบ
- `Report/2026-09-22_2.txt` — รายงานช่วงบ่าย-กลางคืนของเมื่อวาน

---

## ปัญหาที่พบ / ปัญหาที่ยังค้างอยู่ 🔴

| ปัญหา | สาเหตุ | วิธีแก้ / แผน |
|---|---|---|
| ลายเซ็นไม่ไปโผล่บนใบยืมเลย | ตัวสร้าง PDF รับ `BorrowRequestResponse` ไม่ใช่แถว ORM จึงไม่เห็นคอลัมน์ชื่อไฟล์ | เพิ่ม field ใน schema แต่ตั้ง `Field(exclude=True)` — PDF ใช้ได้ แต่ไม่หลุดออก API (มีเทสยืนยัน) |
| ใบยืมสร้างไม่ผ่านเมื่อลายเซ็นเป็นไฟล์เสีย | ReportLab เปิดรูปตอน build เอกสาร ไม่ใช่ตอนสร้าง object | บังคับให้ PIL อ่านไฟล์ใน `_signature_image()` ก่อน ไฟล์เสีย = กลับไปใช้เส้นประเซ็นมือ เอกสารออกได้เสมอ |
| ลายเซ็น/ชื่อ/วันที่บน PDF ไม่ตรงแนวกัน | (1) เส้นประต่อท้าย "ผู้ยืม" ดันจุดกึ่งกลาง (2) `hAlign` ของรูปไม่มีผลใน Table cell | เรียงเป็นแกนเดียว (ลายเซ็น → เส้น → ชื่อ → ตำแหน่ง → วันที่) + สั่ง `ALIGN CENTER` ที่ TableStyle ของแถวรูป — ตรวจด้วยตาแล้ว 3 กรณี |
| `pattern` ของ pydantic ไม่รองรับ look-ahead | rust regex ไม่มี `(?=...)` | ย้ายเงื่อนไข "ต้องมีตัวอักษร" ไปเช็คใน `model_validator` แทน |
| เทสเดิม 2 ตัวล้มหลังเพิ่ม validator | schema เตะ 422 ก่อนถึง service ที่เคยตอบ 400 | แก้เทสให้ส่งรหัสที่ถูกต้อง (เทสลำดับยศต้องพิสูจน์เรื่องยศจริง ไม่ใช่ผ่านเพราะ schema) |
| ผลรวมตารางรายเดือน ≠ ยอดรวมช่วง ในหน้าสถิติ | `borrowed_days_expr(since, until)` ใส่ขั้นต่ำ 1 วัน **ต่อช่วง** ยืมคร่อมเดือนเลยถูกนับ 2 | เสนอทางแก้แล้ว (ขั้นต่ำเฉพาะช่วงที่การยืมเริ่ม) — **ผู้ใช้ให้ปล่อยไว้ก่อน** ยังไม่แก้ |
| งานยังไม่ commit 4 ก้อน | ทำต่อเนื่องหลายเรื่องในสายเดียว | commit แยกเป็น 4 ก้อน: สถิติเมื่อวาน / กระทบยอดสต็อก / รหัสบุคลากร / เฟส 11 |
| scratchpad ถูกล้างระหว่างวัน (อีกครั้ง) | โฟลเดอร์ชั่วคราวของเซสชันหายข้ามรอบ | เขียนสคริปต์แกะ dictionary ใหม่ — ถ้าต้องใช้ซ้ำควรเก็บไว้ใน `Plans/` |

---

## แผนวันพรุ่งนี้ 📋

1. [ ] commit แยก 4 ก้อน แล้วตัดสินใจว่า deploy ขึ้น .130 เลยหรือรอรวมรอบ (migration 0042 ต้องรันบนเครื่องจริงด้วย)
2. [ ] ตัดสินใจเรื่องปุ่ม **"ยกเลิกลายเซ็น" สำหรับ superadmin** (บังคับเหตุผล + audit) — ตอนนี้เซ็นผิดต้องแก้ที่ DB
3. [ ] ให้ผู้ยืมเห็นลายเซ็นของตัวเองในหน้า `MyBorrowsPage.jsx` (ตอนนี้ดูได้เฉพาะฝั่งแอดมิน)
4. [ ] ทดสอบเซ็นจริงบนแท็บเล็ต/มือถือ (pointer events + `touch-action`) ก่อนใช้งานหน้าเคาน์เตอร์
5. [ ] งานค้างจากเมื่อวาน: `backend/scripts/backup_db.sh` สำรอง `private_uploads_data` + cron รายคืนบน .130
       (ตอนนี้มีไฟล์ลายเซ็นเพิ่มเข้ามาในโฟลเดอร์ส่วนตัวแล้ว ยิ่งต้องสำรอง)

---

## สถานะโดยรวม

**Backend:** ~99% — ฟีเจอร์ตามแผนครบถึงเฟส 11 เหลืองานดูแลระบบ (สคริปต์สำรองข้อมูล) และของที่ยัง commit ไม่ครบ
**Frontend:** ~99% — เหลือให้ผู้ยืมดูลายเซ็นตัวเอง + polish จากการใช้จริง
**ระยะรวม:** ระบบพร้อมใช้งานจริงและ deploy แล้วบนเครื่องใหม่ (172.16.46.130) เวอร์ชันบนเครื่องยังเป็น v2.0.1
ส่วนงานวันนี้ (เฟส 11 + ชุดเล็ก) ยังอยู่บนเครื่อง dev เท่านั้น

---

## หมายเหตุ / ความเสี่ยง ⚠️

- **ไฟล์ลายเซ็นเป็นข้อมูลส่วนบุคคล** — เก็บใน `PRIVATE_UPLOAD_DIR` ที่ไม่ได้เสิร์ฟ static เปิดได้ทางเดียวคือ
  endpoint ที่ตรวจสิทธิ์ + ลง audit ทุกครั้ง **ห้ามย้ายไป `/uploads`** (กฎเดียวกับใบยืมที่เซ็นแล้ว)
  และการสำรองข้อมูลต้องครอบโฟลเดอร์นี้ด้วย ไม่งั้นกู้ระบบมาแล้วลายเซ็นหาย
- **migration 0042 ต้องรันบน .130 ตอน deploy** — ถ้าลืม backend จะพังทันทีเพราะ model อ้างคอลัมน์ที่ยังไม่มี
- ข้อมูลใน `docs/MNKPJ/` เป็นฐานข้อมูลจริงของระบบภายนอก (มีข้อมูลลูกค้า) — **ห้าม commit ขึ้น repo public**
  ตอนนี้ยังอยู่ใน working tree ต้องเพิ่มใน `.gitignore` ก่อน commit รอบหน้า
- Pending Items จาก CLAUDE.md ที่ยังค้างเหมือนเดิม: ยืนยัน `ALLOWED_EMAIL_DOMAINS` ก่อนเปิดใช้จริง ·
  LINE OA ยังไม่ทำ · Export Excel ยังอยู่นอกขอบเขต (แต่คู่เทียบมีกันทุกเจ้า ควรพิจารณาใหม่)
