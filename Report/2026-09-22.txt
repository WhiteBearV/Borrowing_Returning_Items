# รายงานความคืบหน้า — วันที่ 22 กันยายน 2569
**โปรเจค:** ระบบยืม-คืนอุปกรณ์ (Equipment Borrowing System)
**ผู้รายงาน:** Wiraphat

สรุปสั้น: เจอบั๊กสิทธิ์ร้ายแรง ผู้ดูแลคลัง (admin) ปิดบัญชีผู้ดูแลระบบสูงสุด (superadmin) ได้ แก้ด้วยกฎลำดับยศจุดเดียว
และไล่หาจุดที่มีปัญหาแบบเดียวกันเจอเพิ่มอีก 5 จุด (สร้างบัญชี superadmin ผ่าน API ได้, จัดการค่าปรับของตัวเองได้ ฯลฯ)
จากนั้นเขียน Patch Notes เทียบเวอร์ชันบน prod เก่ากับเวอร์ชันปัจจุบัน และเขียนคู่มือติดตั้ง/ตั้งค่าทั้งหมดใหม่ให้ตรงกับของจริง
เทสเพิ่มจาก 457 เป็น 462 ผ่านทั้งหมด · commit + push `84442be` (v2.0.1)
**deploy ขึ้นเครื่องใหม่ DockerMeab02 (172.16.46.130) แล้ว** · สำรองข้อมูลก่อนอัปเดต · ข้อมูลครบทุกตาราง

---

## สิ่งที่ทำสำเร็จวันนี้ ✅

### Backend — แก้บั๊กสิทธิ์ (v2.0.1)
- **ต้นเหตุ:** endpoint จัดการผู้ใช้กั้นแค่ `require_admin` ไม่ได้ดูยศของบัญชีเป้าหมาย ผู้ดูแลคลังจึงทำสิ่งต่อไปนี้ได้:
  - ปิดบัญชี / ปฏิเสธการอนุมัติ / แก้ชั้นปี ของ superadmin
  - เมื่อ superadmin ถูกปิด บัญชีจะใช้งานไม่ได้ทันที เพราะ `get_current_user` เช็ค `is_active`
  - ไม่มีใครยศสูงพอจะเปิดบัญชีคืนให้ได้
- `utils/roles.py`
  - เพิ่ม `ROLE_RANK` และ `role_rank()`
  - เพิ่ม `can_manage_user(actor, target)` เป็น**จุดเดียว**ที่ตัดสินสิทธิ์ ใช้กับ เปิด/ปิดบัญชี, อนุมัติผู้สมัคร และแก้ชั้นปี
  - กติกา: จัดการได้เฉพาะยศที่ต่ำกว่าตัวเอง · superadmin จัดการได้ทุกคน · ผู้ดูแลคลังด้วยกันจัดการกันเองไม่ได้
- `services/users_service.py`
  - เพิ่ม `_assert_can_manage()` เรียกใน `update_status` / `update_approval` / `update_study`
  - `create_user`: สร้างบัญชีได้ไม่เกินยศตัวเอง (เดิมหน้าเว็บไม่มีตัวเลือก superadmin แต่ยิง API ตรงได้ = ยกระดับตัวเองได้)
  - `update_status`: ห้ามปิดบัญชีตัวเอง
  - `_assert_not_last_superadmin()` (ใช้ร่วมกับ `update_role`): ห้ามปิดหรือลดยศ superadmin คนสุดท้ายที่ยังใช้งานได้
  - `update_approval`: ใช้ได้กับบัญชี `student` เท่านั้น เพราะ "ปฏิเสธ" บัญชีเจ้าหน้าที่ = ล็อกเอาต์เขาทางอ้อม
- `services/borrow_service.py` — ค่าปรับ
  - `_assert_not_own_fine()`: เจ้าหน้าที่แก้ยอด / รับชำระ / ยกเว้น / กรอกทับตอนรับคืน ในคำขอที่ตัวเองเป็นผู้ยืมไม่ได้
  - `update_fine` และการกรอกทับใน `return_item`: แก้จนยอดเหลือ 0 ไม่ได้
    - เหตุผล: เดิมสถานะพลิกเป็น `none` แล้วหายจากรายงาน = ยกเว้นค่าปรับทางอ้อมโดยไม่ต้องเป็น superadmin
    - ต้องใช้ปุ่ม "ยกเว้นค่าปรับ" ของ superadmin แทน
- ตรวจจุดอื่นที่อาจรั่วแบบเดียวกันแล้วว่าปลอดภัย:
  - settings (แยกสิทธิ์รายคีย์)
  - ฟิลด์การเงิน (`FINANCE_FIELDS` ทั้งแก้รายชิ้นและหลายรายการ)
  - นำเข้าไฟล์ทะเบียน (เติมเฉพาะช่องว่าง)
  - endpoint ที่กั้นด้วย `require_superadmin` ทั้งหมด
- เทส
  - ใหม่: `test_user_hierarchy.py` (4 เทส), `test_fines.py::test_staff_cannot_zero_fine_or_handle_own_fine`
  - แก้: `test_user_delete.py` ให้ใช้ token ของ superadmin ตอนปิดบัญชี admin
  - ผล: **462 passed**

### Frontend
- `utils/role.js`: เพิ่ม `canManageUser()` เป็นคู่แฝดของ backend ใช้ซ่อนปุ่มเท่านั้น ตัวบังคับจริงอยู่ที่ backend
- `pages/admin/UsersPage.jsx`:
  - ปุ่มเปิด/ปิดบัญชีและปุ่มแก้ชั้นปีโชว์เฉพาะแถวที่มีสิทธิ์จัดการ
  - ปุ่มเปิด/ปิดบัญชีไม่โชว์ในแถวของตัวเอง

### อื่น ๆ (Docs, Deploy)
- **`Report/2026-09-21.txt`**: รายงานของเมื่อวาน
- **`PATCH_NOTES.md`** (ใหม่): เทียบโค้ดบน prod เก่า `695f9b7` กับเวอร์ชันปัจจุบันจาก git เขียนแบบ patch notes เกม
  - v2.0 และ hotfix v2.0.1
  - แยกตามผู้ใช้แต่ละกลุ่ม พร้อมสิ่งที่ต้องทำหลังอัปเดต (สร้าง SuperAdmin, นำเข้ารายชื่อนักศึกษา)
- **คู่มือติดตั้ง/ตั้งค่าเขียนใหม่ให้ตรงเวอร์ชันปัจจุบัน:**
  - `README.md` เป็นคู่มือหลักไฟล์เดียว 11 หัวข้อ: dev, `.env` ทุกตัวแปร, ติดตั้ง prod, อัปเดต/ย้อนกลับ, ตาราง settings + ใครแก้ได้, backup/restore, เทส, แก้ปัญหา
  - `HANDOFF.md` และ `PROJECT_OVERVIEW.txt` เหลือแค่ภาพรวม แล้วชี้ไปที่ README กันข้อมูลซ้ำซ้อนจนไม่ตรงกัน
  - `DEPLOY-PLAN.md` ติดป้ายว่าเป็นเอกสารประวัติ
- คู่มือเดิมผิดจากของจริงหลายจุด แก้แล้ว:
  - `python -m app.main` ไม่ได้เปิดเซิร์ฟเวอร์ (main.py ไม่มี `__main__`) เปลี่ยนเป็น `uvicorn`
  - `docker compose up --build` บนเครื่อง dev ล้ม เพราะ frontend ใช้ nginx HTTPS ที่หาไฟล์ cert ไม่เจอ เปลี่ยนเป็นเปิดแค่ `db`
  - ยังบอกให้แทรก SQL เองเพื่อสร้าง admin คนแรก เปลี่ยนเป็น `scripts/create_admin.py --superadmin`
  - ยังเขียนว่าวัสดุสิ้นเปลืองคืนอัตโนมัติ ซึ่งไม่จริงแล้ว
- `.env` ตัวอย่าง (`.env.prod.example`, `backend/.env.example`, `frontend/.env.example` เป็น `VITE_API_URL=/api`) ตรงกับตัวแปรที่ใช้จริง
  - ย้ายคำอธิบายทั้งหมดไปไว้บรรทัดของตัวเอง ไม่มี inline comment เพราะ docker compose อ่านค่าเพี้ยนได้
- `CLAUDE.md`: เพิ่มกฎลำดับยศในการจัดการบัญชี และกฎค่าปรับของตัวเอง (หัวข้อ 6) · แก้คำสั่งรันในหัวข้อ 8
- commit + push `84442be` รวม `บันทึกการพบที่ปรึกษา.txt` (บันทึก 10–11 ก.ย.)
- **Deploy ขึ้นเครื่องใหม่ DockerMeab02 (172.16.46.130)** เครื่องใหม่ที่ได้ IP เดิม
  - สภาพก่อนอัปเดต: โค้ด `1da1c22`, DB อยู่ 0041, `.env`/cert ตั้งเป็น .130 ครบ, container ทำงานปกติ
  - สำรองก่อนอัปเดตที่ `/root/backups/pre-update-20260922-0736/`
    - DB dump: 16 ตารางมีข้อมูล
    - uploads: 195 ไฟล์
    - private_uploads: ว่าง
    - `.env`, certs และเลข commit เดิม
  - `git pull` เป็น `84442be` แล้วใช้ `up -d --build --no-deps backend frontend` (ไม่มี migration ใหม่ ไม่แตะ db)
  - ตรวจหลังอัปเดต:
    - backend healthy · `/api/health` ok · หน้าเว็บ 200 · log ไม่มี error
    - จำนวนแถวเท่าเดิม: users 48 · superadmin 1 · equipment 1557 · คำขอ 147 · รายการยืม 375 · audit 793
- ตอบคำถาม: อัปเดตเล็กโดยไม่ build ใหม่ทั้งก้อนใช้ `--no-deps backend frontend` ร่วมกับ layer cache
  - ห้ามใช้ `down -v` (ลบ volume ข้อมูล) และ `--no-cache`

---

## ปัญหาที่พบ / ปัญหาที่ยังค้างอยู่ 🔴

| ปัญหา | สาเหตุ | วิธีแก้ / แผน |
|---|---|---|
| ผู้ดูแลคลังปิดบัญชี superadmin ได้ (ร้ายแรง) | endpoint จัดการผู้ใช้เช็คแค่ยศของคนกด ไม่ได้เช็คยศของบัญชีเป้าหมาย | **แก้แล้ว** `can_manage_user` จุดเดียว + เทส 4 ตัว · ขึ้น .130 แล้ว |
| เจ้าหน้าที่จัดการ/ลบค่าปรับของตัวเองได้ | ไม่มีการเช็คว่าผู้กดเป็นผู้ยืมเอง · แก้ยอดเป็น 0 ได้ | **แก้แล้ว** `_assert_not_own_fine` + ห้ามยอด 0 |
| เจ้าหน้าที่ยังอนุมัติคำขอยืมของตัวเองได้ | เดิมตั้งใจให้ admin ยืมเองได้ (19 ก.ค.) | ถามผู้ใช้แล้วว่าจะบล็อกไหม ยังไม่ได้คำตอบ |
| script สำรองข้อมูลหยุดกลางทางบน .130 | น่าจะเป็น `docker compose exec` อ่าน stdin ของสคริปต์ที่ส่งผ่าน ssh ไปหมด คำสั่งถัดไปเลยไม่ได้รัน | รันขั้นที่เหลือใหม่โดยใส่ `< /dev/null` แล้วผ่าน · ไฟล์ครบ ตรวจแล้ว |
| .130 ยังไม่มีการสำรองอัตโนมัติ (cron) | เครื่องใหม่ ยังไม่ได้ตั้ง | เสนอตั้ง cron ทุกคืนแล้ว รอผู้ใช้ตกลง |
| `backup_db.sh` ยังไม่สำรอง `private_uploads_data` + ทิ้งไฟล์ 0 ไบต์ตอนล้ม | สคริปต์เขียนก่อนมีฟีเจอร์ใบยืมที่เซ็นแล้ว | ค้างจากเมื่อวาน · ควรแก้ก่อนตั้ง cron |
| `eligible_students` บน .130 = 0 คน | เครื่องใหม่ยังไม่ได้นำเข้าไฟล์รายชื่อ | ผู้สมัครใหม่ทุกคนจะไปรออนุมัติ ต้องนำเข้าไฟล์รายชื่อที่หน้าเว็บ |
| นาฬิกาเครื่อง .130 เป็น UTC | ค่าเริ่มต้นของ Ubuntu | ระบบไม่กระทบ (container ตั้ง TZ แล้ว) · กระทบแค่เวลา cron/log ของเครื่อง · เสนอ `timedatectl` |
| เครื่องเก่า .134 (DockerMaeb) ยังเปิดอยู่ที่ `695f9b7` | ผู้ใช้ย้ายมาใช้เครื่องใหม่ | แนะนำให้ปิด กันผู้ใช้เข้าผิดเครื่องแล้วข้อมูลแยกกัน |

---

## แผนวันพรุ่งนี้ 📋

1. [ ] แก้ `backend/scripts/backup_db.sh` ให้สำรอง `private_uploads_data` + ลบไฟล์ 0 ไบต์ / exit ≠ 0 เมื่อสำรองไม่สำเร็จ
2. [ ] ตั้ง cron สำรองข้อมูลทุกคืนบน .130 + `timedatectl set-timezone Asia/Bangkok` (ถ้าผู้ใช้ตกลง)
3. [ ] นำเข้าไฟล์รายชื่อนักศึกษาที่รับรองบน .130 (ผู้ใช้ทำผ่านหน้าเว็บ)
4. [ ] QA สิทธิ์บน .130 ด้วยบัญชีจริง: ผู้ดูแลคลังต้องไม่เห็นปุ่มปิดบัญชี superadmin/admin คนอื่น · ยิง API ตรงต้องได้ 403
5. [ ] ตัดสินใจเรื่องบล็อกเจ้าหน้าที่อนุมัติคำขอของตัวเอง → ถ้าบล็อก เพิ่มเช็คใน `borrow_service.approve_request` + เทส
6. [ ] เพิ่มหัวข้อ "อัปเดตเล็ก (ไม่มี migration)" ใน README หัวข้อ 6

---

## สถานะโดยรวม

**Backend:** ~99% · เทส 462 ผ่าน · เหลือแก้สคริปต์ backup และทำ backup off-site
**Frontend:** ~98% · เหลือ QA หน้าจัดการผู้ใช้บนเครื่องจริงด้วยบัญชีผู้ดูแลคลัง
**ระยะรวม:** v2.0.1 ขึ้นเครื่องใช้งานจริง (.130) แล้ว · คู่มือตรงกับโค้ด · เหลืองานดูแลระบบ (backup อัตโนมัติ, รายชื่อนักศึกษา)

---

## หมายเหตุ / ความเสี่ยง ⚠️

- **พฤติกรรมที่เปลี่ยนใน v2.0.1** ต้องแจ้งเจ้าหน้าที่:
  - ผู้ดูแลคลังเปิด/ปิดบัญชีหรือแก้ชั้นปีของผู้ดูแลคลังคนอื่นไม่ได้แล้ว ต้องให้ superadmin ทำ
  - ผู้ดูแลคลังแก้ยอดค่าปรับเป็น 0 ไม่ได้ ต้องขอ superadmin ยกเว้นค่าปรับแทน
- **superadmin บน .130 มีคนเดียว** ระบบกันไม่ให้ปิดหรือลดยศคนสุดท้ายแล้ว แต่ถ้าลืมรหัสผ่าน ต้องรีเซ็ตผ่านอีเมล หรือใช้สคริปต์บนเซิร์ฟเวอร์
- backup ยังอยู่บน VM เครื่องเดียว (ไม่มี off-site) · repo GitHub เป็น public ห้าม commit ไฟล์รายชื่อนักศึกษา / `.env` / ไฟล์สำรอง
- Pending Items (CLAUDE.md หัวข้อ 10): ยืนยัน `ALLOWED_EMAIL_DOMAINS` กับอาจารย์ · LINE OA ยังเป็น stretch goal
