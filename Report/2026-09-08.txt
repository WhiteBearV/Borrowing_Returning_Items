# รายงานความคืบหน้า — วันที่ 8 กันยายน 2569
**โปรเจค:** ระบบยืม-คืนอุปกรณ์ (Equipment Borrowing System)
**ผู้รายงาน:** Wiraphat

สรุปสั้น: ปิดแผน feedback อาจารย์ 5 ก.ย. **ครบทั้ง 10 เฟส (0–9)** และแก้ feedback ผู้ใช้อีก 25 ข้อ
เทสอัตโนมัติ 394 ตัวผ่านทั้งหมด (เพิ่มจากเมื่อวาน 366 → 394) · migration ถึง 0039

---

## สิ่งที่ทำสำเร็จวันนี้ ✅

### Backend

**เฟส 6 — ค่าปรับล่าช้า + ค่าเสียหาย (feedback อาจารย์ ข้อ 7)**
- migration `0034_borrow_item_fines.py` — คอลัมน์ `fine_days_late / fine_late_amount /
  fine_damage_amount / fine_basis / fine_status / fine_waived_by / fine_waived_reason / fine_waived_at`
  + settings `fine_per_day_per_item` (10) · `fine_grace_days` (0) · `fine_max_per_item` (0)
- `borrow_service._compute_fine()` (ฟังก์ชันบริสุทธิ์) + `_apply_fine()` — เรียกทั้งใน `return_item()`
  และ `return_all_items()` · **ยอดถูก freeze ตอนรับคืน ไม่คำนวณสดตอนอ่าน** (อัตราใน settings เปลี่ยนได้
  ยอดของเคสเก่าต้องไม่ขยับ = หลักการเดียวกับ `unit_value_snapshot`)
- ค่าเสียหายใช้ `book_value_snapshot` (มูลค่าตามบัญชี) ไม่ใช่ราคาที่ซื้อ และไม่ใช่สถิติการใช้งาน
- สถานะเดินทางเดียว `none → unpaid → (paid | waived)` บังคับที่ `_load_fine_item()` จุดเดียว
- endpoint ใหม่: `PATCH /borrow-requests/{id}/items/{item_id}/fine` (แก้ยอด, staff) ·
  `/fine/pay` (staff) · `/fine/waive` (**superadmin เท่านั้น**) · `GET /dashboard/fines` + `/fines/export` (CSV มี BOM)

**เฟส 7 — ปิดช่องโหว่ไฟล์ใบยืมที่เซ็นแล้ว (ข้อ 16)**
- migration `0035_signed_form_private_storage.py` — เปลี่ยนชื่อคอลัมน์ `signed_form_url` →
  `signed_form_file` (เก็บชื่อไฟล์ ไม่ใช่ URL)
- ไฟล์ย้ายไป `settings.PRIVATE_UPLOAD_DIR` ที่ไม่ได้ mount เป็น StaticFiles ·
  `GET /borrow-requests/{id}/signed-form` ตรวจสิทธิ์ (เจ้าของ/staff) + ลง audit `view_signed_form`
- **ตรวจ magic bytes ที่ด่านอัปโหลดกลาง** `equipment_service._read_validated()` = ทุกการอัปโหลดในระบบ
  ได้ผลทันที (กัน .html/.php เปลี่ยนนามสกุลเป็น .jpg แล้วถูกเสิร์ฟจาก /uploads = stored XSS)
- เพิ่ม volume `private_uploads_data` ใน docker-compose ทั้ง dev/prod

**เฟส 8 — ฟอร์มอุปกรณ์ + มาตรฐานการตั้งชื่อ (ข้อ 15, 17, 18)**
- migration `0036_equipment_naming_and_part_replaces.py` — `equipment.manufacturer` /
  `equipment.model_number` (index) + `equipment_parts.replaces_part_id` (self-FK)
- `equipment_service._search_clause()` — ค้นหารวมที่เดียว ครอบ ชื่อ/รหัส/รุ่น/ผู้ผลิต/SN
- `FINANCE_FIELDS` + `_assert_can_edit_finance()` — ราคา/วันที่ได้มา/อายุ/มูลค่าตามบัญชี
  แก้ได้เฉพาะ superadmin **บังคับทั้ง `update_equipment` และ `bulk_update_equipment`**
- `EquipmentUpdate.status_reason` — เปลี่ยนสถานะต้องมีเหตุผลเสมอ (ลง audit)
- `borrow_service._attach_equipment_specs()` + `pdf.py` — ใบยืมพิมพ์สเปกชิ้นส่วนที่ติดตั้ง ณ วันยืม

**เฟส 9 — รายชื่อ นศ. ที่สาขารับรอง (ข้อ 3) — ได้ไฟล์ตัวอย่างจากผู้ใช้แล้ว**
- migration `0037_eligible_students.py` — ตาราง `eligible_students` + `users.approval_status/approval_note`
- `student_import_service.py` (ใหม่) อ่านไฟล์ `repofficeradvisor.xls` ของสำนักทะเบียนได้จริง
  รองรับ **.xls (xlrd) / .xlsx (openpyxl) / .csv / .pdf (pypdf → ตกไป OCR ถ้าเป็นไฟล์สแกน)**
  · อ่าน **คณะ/สาขา/รุ่น/อาจารย์ที่ปรึกษาจากหัวกระดาษ** ไม่ใช่แค่ตารางรายชื่อ
  · นำเข้าซ้ำ = อัปเดตทับด้วย `student_id` **ห้ามล้างตาราง** (1 ไฟล์ = 1 อาจารย์ที่ปรึกษา)
- `auth_service.register()` — รหัส+ชื่อตรงรายชื่อ → **ใช้ชื่อ/สาขาจากรายชื่อทับที่ผู้สมัครกรอก** ·
  ไม่ตรง/ไม่มีในรายชื่อ → `approval_status=pending` + แจ้งเตือนเจ้าหน้าที่ (ไม่ปิดตาย)
- `users_service.update_approval()` + `PATCH /users/{id}/approval` · `routers/eligible_students.py` (ใหม่)

**แก้ตาม feedback ผู้ใช้ 25 ข้อ (รอบบ่าย–เย็น)**
- migration `0038_cancel_reason_and_last_seen.py` — `borrow_requests.cancel_reason` (แยกจาก
  `rejection_reason` ของแอดมิน) + `users.last_seen_at` · `0039_setting_descriptions.py` เก็บคำอธิบาย settings
- `settings_service.ADMIN_EDITABLE_KEYS` — ผู้ดูแลคลังแก้ settings งานประจำได้ (นัดรับ/แจ้งเตือน/สต็อก/โควต้า)
  ที่เหลือ (ค่าปรับ/ค่าเสื่อม/มูลค่าในเอกสาร) ยังเป็นของ superadmin
- ลบอุปกรณ์ถาวร (เดี่ยว/หลายรายการ) → **ผู้ดูแลคลังทำได้** ตามที่ผู้ใช้สั่ง (เงื่อนไขเดิมยังอยู่: ต้องปลดระวางก่อน)
- `users_service.update_profile()` ไม่รับ `major` อีกต่อไป — ผู้ใช้แก้สาขาตัวเองไม่ได้
- `dashboard_service.get_summary()` + schema — ภาพรวมผู้ใช้ (รวม/แยกสาขา/เจ้าหน้าที่/รออนุมัติ)
- `system_check_service._health_checks()` — ตรวจระบบจริง: ต่อ DB ติดไหม+กี่ ms · เวอร์ชัน migration ·
  ขนาด DB · uptime · **งานแจ้งเตือนอัตโนมัติยังเดินไหม + รอบถัดไป** · สถานะอีเมล ·
  **ผู้ใช้ที่ใช้งานอยู่ 15 นาทีล่าสุด** · พื้นที่ดิสก์ · ขนาดไฟล์แนบ · งานค้างรอเจ้าหน้าที่
- `pdf.py` — เลิกเติมแถวว่างในใบยืม/ใบคืน (ตัด `_MIN_ITEM_ROWS`) และใบขออนุมัติซ่อม
- `equipment_service.list_equipment_grouped()` + `_summarize_cards()` — สรุปจำนวน **ชิ้นจริง**
  แยกตามประเภท (ตารางยุบรุ่นเดียวกันเป็นแถวเดียว "187 รายการ" ทำให้เข้าใจผิดว่าคลังมีของแค่ 187 ชิ้น)

### Frontend

- **หน้า "คำขอยืมของฉัน" รื้อใหม่** — เต็มความกว้าง + การ์ดแบ่งส่วน (หัว / รายการ / เอกสาร / การจัดการ)
  · ป้าย "เกินกำหนด N วัน" ทั้งระดับใบและ**รายชิ้น** · อัปโหลดใบเซ็นแล้วปุ่มเปลี่ยนเป็น "ดูใบยืมที่เซ็นแล้ว"
  พร้อมวันเวลาอัปโหลด · "ดูใบรับคืน" ย้ายลงบรรทัดใหม่พร้อมเวลาอัปเดตล่าสุด (ของชิ้นสุดท้ายที่รับคืน)
- **มินิโปรไฟล์มุมขวาบน** (`ProtectedRoute.jsx`) เรียง กระดิ่ง → รูป → ชื่อ → สิทธิ์ กดไปหน้าโปรไฟล์ได้
- `components/common/ReasonModal.jsx` (ใหม่) — ใช้ร่วมทั้ง **ยกเลิกคำขอ** (ผู้ยืม) และ **ปฏิเสธคำขอ** (แอดมิน)
  มีดรอปดาวน์เหตุผลสำเร็จรูป + autofill + ตัวเลือก "อื่น ๆ"
- หน้าใหม่ `EligibleStudentsPage.jsx` (นำเข้า/ค้นหา/ถอนรายชื่อ) · `FinesPage.jsx` (สรุปค่าปรับ + CSV)
- `SettingsPage.jsx` — เต็มจอ + จัดกลุ่ม + **เอาชื่อ key ดิบออก** + `pdf_value_source` เป็นดรอปดาวน์
  + ค่าที่แก้ไม่ได้ขึ้นเทาพร้อมเหตุผล
- `AuditLogPage.jsx` — ตัวกรองสิทธิ์ดึงจาก `utils/role.js` (**เพิ่ม SuperAdmin ที่หายไป**) ·
  ประโยค log แยกบรรทัด (หัวเรื่อง + รายละเอียดเป็นข้อ ๆ) · โมดัลรายละเอียดขยายเป็น `max-w-3xl`
- `EquipmentManagePage.jsx` — สรุป "1,621 ชิ้น จาก 187 รุ่น" + ชิปแยกประเภท · ป้ายเกินกำหนดข้างชื่อผู้ยืม ·
  ฟอร์ม 3 แท็บใช้ได้ทั้งตอนเพิ่ม/แก้ไข · เรียงช่องตามทะเบียนพัสดุ (รหัส → ชื่อ → ผู้ผลิต → รุ่น → SN → สถานที่)
  · ตัดช่อง "ชุดอุปกรณ์" ออก (ไปจัดการที่หน้าชุดอุปกรณ์อย่างเดียว) · bulk edit ซ่อนฟิลด์การเงินให้ผู้ดูแลคลัง
- `UsersPage.jsx` — แถบคิวผู้สมัครรออนุมัติ (อนุมัติ/ปฏิเสธ) + จำนวนบัญชีทั้งหมด
- `DashboardPage.jsx` — บล็อก "ภาพรวมผู้ใช้งาน" แยกตามสาขา · `SystemCheckPage.jsx` — การ์ดสถานะระบบ
- `ProfilePage.jsx` — สาขาเป็นข้อความอ่านอย่างเดียว

### อื่น ๆ (Config, Docker, Docs)
- `requirements.txt` +`xlrd>=2.0.1` · `.env.example` + `PRIVATE_UPLOAD_DIR` · `.gitignore` + `backend/private_uploads/`
- `docker-compose.yml` / `.prod.yml` + volume `private_uploads_data`
- `docs/naming-convention.md` (ใหม่) — กฎตั้งชื่อ + ตัวอย่าง ❌/✅ + ที่มา 5 แหล่ง
- `CLAUDE.md` + `docs/02-system-design.md` อัปเดตครบทุกเฟส (schema/endpoint/สิทธิ์/กฎใหม่)
- เทสใหม่: `test_fines.py` (8) · `test_equipment_naming_and_permissions.py` (5) ·
  `test_eligible_students.py` (8) + เทสย่อยในไฟล์เดิม — รวม **394 ผ่าน**

---

## ปัญหาที่พบ / ปัญหาที่ยังค้างอยู่ 🔴

| ปัญหา | สาเหตุ | วิธีแก้ / แผน |
|---|---|---|
| ปรับยอดคงเหลือหลายรายการ "ไม่ได้ทั้งลดและเพิ่ม" | ติ๊กทั้งรุ่น = เลือกหน่วยย่อย 42 แถว (แถวละ 1 ชิ้น) แล้ว delta ถูกใช้กับ*ทุกแถว* → -1 = ทั้งรุ่นเหลือ 0, +1 = ชน clamp ที่ 1 จนดูเหมือนไม่มีอะไรเกิด | แก้แล้ว: แถว `quantity_total == 1` คิดแบบ **กองรวม** (-3 = ปิด 3 ชิ้น / +3 = เปิดคืน 3 ชิ้น) ของนับเป็นก้อนยังบวก/ลบตรง ๆ + เทสกันซ้ำ |
| ติ๊กเลือกหลายรายการแล้วกดยุบกลุ่ม ติ๊กหาย | สถานะติ๊กของแถวกลุ่มอ่านจาก `expanded[id]` ที่ถูกลบตอนยุบ | แก้แล้ว: แยก `memberCache` (จำรายชื่อหน่วย) ออกจาก `expanded` (กำลังกางอยู่) |
| กรอง log ของ SuperAdmin ไม่เจอ ทั้งที่ log ปรากฏ | dropdown สิทธิ์เขียนมือไว้แค่ admin/student | แก้แล้ว: ดึงจาก `ALL_ROLES` ใน `utils/role.js` |
| ระหว่างทดสอบทำสต็อก Arduino UNO R3 เพี้ยน (36/42 → 0/42) | ทดสอบบั๊กปรับยอดกับข้อมูล dev จริง | **กู้คืนครบแล้วจาก audit log** (`old_available` รายแถว) ตรวจซ้ำ = 36/42 เท่าเดิม + ลบ log ทดสอบทิ้ง |
| เทส `test_quota_limit` พังแบบสุ่ม | อ่านค่า `max_active_requests_per_student` จาก DB dev ที่ผู้ใช้ปรับเป็น 5 | แก้แล้ว: เทสตั้งค่าเองแล้วคืนค่าเดิมใน `finally` |
| ยังไม่ commit เลยสักครั้ง | งานสะสมตั้งแต่เฟส 0 (2 ก.ย.) | **167 ไฟล์ · migration 0025–0039 รอผู้ใช้สั่ง commit** |
| ของในคลัง 697 แถวยังไม่มีราคา · 1,491 แถวไม่มีวันที่ได้มา | ข้อมูลนำเข้าจากทะเบียนเดิมไม่ครบ | ค่าเสื่อม/ค่าเสียหายคำนวณไม่ได้จนกว่าจะ backfill — งานข้อมูล ไม่ใช่งานโค้ด |

---

## แผนวันพรุ่งนี้ 📋

1. [ ] commit งานทั้งหมด (แยกเป็นก้อนตามเฟส 6/7/8/9 + ก้อน feedback ผู้ใช้) แล้ว push
2. [ ] ขอไฟล์รายชื่อ นศ. ให้ครบทุกอาจารย์ที่ปรึกษาจากสาขา แล้วนำเข้าจริง (ตอนนี้มีแค่ไฟล์ตัวอย่าง 3 คน)
3. [ ] ถามสาขาว่ารหัส "สถานะ" (11/12) ในไฟล์รายชื่อหมายถึงอะไร — ถ้ามีรหัสพ้นสภาพ ต้องกรองออกจาก whitelist
4. [ ] QA รอบเต็มด้วยบัญชีจริงทั้ง 3 ระดับ โดยเฉพาะเส้นทางค่าปรับ (รับคืนช้า → แก้ยอด → รับชำระ → ยกเว้น)
5. [ ] backfill ราคา/วันที่ได้มาของอุปกรณ์เดิม (เริ่มจากครุภัณฑ์ที่ถูกยืมบ่อย) + ไล่แก้ชื่อที่เป็นชื่อรุ่นล้วน
   ด้วยปุ่ม "แก้ไขหลายรายการ" (มีช่องผู้ผลิต/รุ่นแล้ว)

---

## สถานะโดยรวม

**Backend:** ~99% — โค้ดครบทุกเฟสตามแผน เหลือแค่ปรับตาม feedback รอบถัดไปและงาน backup off-site
**Frontend:** ~98% — ครบทุกหน้าแล้ว เหลือขัดรายละเอียด UX ที่เจอเพิ่มระหว่างใช้จริง
**ระยะรวม:** แผน feedback อาจารย์ 5 ก.ย. (17 หัวข้อ / 10 เฟส) **ปิดครบแล้ว** และ feedback ผู้ใช้ 25 ข้อ
ปิดครบเช่นกัน · ขั้นถัดไปคือเก็บข้อมูลจริง (รายชื่อ นศ. + ราคา/วันที่ได้มา) แล้วทดสอบใช้งานจริง

---

## หมายเหตุ / ความเสี่ยง ⚠️

- **ยังไม่ commit** — งาน 167 ไฟล์ค้างใน working tree ทั้งหมด ถ้าเครื่องมีปัญหาก่อน commit งานหายทันที
- **ALLOWED_EMAIL_DOMAINS** (CLAUDE.md ข้อ 10) — ยังต้องยืนยันโดเมนจริงกับอาจารย์ก่อน deploy
- **ไฟล์รายชื่อ นศ.** — ได้ตัวอย่างแล้ว (`repofficeradvisor.xls`) แต่ยังไม่ได้ชุดจริงทั้งสาขา
  ระบบพร้อมรับแล้ว นำเข้าซ้ำได้เรื่อย ๆ โดยไม่ลบของเดิม
- **LINE OA** — ยัง stretch goal เหมือนเดิม ยังไม่มี Channel Access Token
- **ตัดสินใจที่เปลี่ยนจากแผนเดิม:** ลบอุปกรณ์ถาวรย้ายจาก SuperAdmin → ผู้ดูแลคลัง (ผู้ใช้สั่ง 8 ก.ย.)
  และ settings แยกสิทธิ์รายคีย์แทนที่จะกันทั้งหน้า — อัปเดต CLAUDE.md + เทสสิทธิ์ให้ตรงแล้ว
- **การทดสอบยิงใส่ DB dev จริงที่มีคนใช้ร่วม** — วันนี้เกือบพลาดจากการทดสอบบั๊กปรับยอดกับข้อมูลจริง
  ครั้งหน้าให้สร้างแถวทดสอบของตัวเองก่อนเสมอ (เหมือนที่ทำกับเฟส 6–9)
