# /api = ยิงผ่าน proxy ของ vite ไปที่ backend localhost:8000 (ดู vite.config.js)
# ทำให้มือถือ/เครื่องอื่นในวง LAN เข้าผ่าน http://<IP-เครื่อง>:5173 ได้ และไม่ต้องตั้ง CORS
# production ไม่ใช้ไฟล์นี้ — docker-compose.prod.yml ส่ง VITE_API_URL=/api ให้ตอน build
VITE_API_URL=/api
